# Pinned to the versions resolved on 2026-07-06 (Python 3.11) for reproducible
# setups. To refresh: recreate the venv, `pip install` the unpinned names, then
# `pip freeze` and update the pins here.
requests==2.34.2
httpx==0.28.1
selectolax==0.4.10
beautifulsoup4==4.15.0
pandas==3.0.3
python-dotenv==1.2.2
apify-client==3.0.4
python-whois==0.9.6
playwright==1.61.0
anthropic==0.116.0
curl_cffi==0.15.0
